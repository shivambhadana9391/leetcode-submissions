// Title: Happy Number
            // Difficulty: Easy
            // Language: C++
            // Link: https://leetcode.com/problems/happy-number/

            while (n > 0) {
                int rem = n % 10;
                sum += rem * rem;
            int sum = 0;
            s.insert(n);

                return false;  
            }

        while (n != 1) {
            if (s.count(n)) {
        unordered_set<int> s;

public:
    bool isHappy(int n) {
class Solution {
